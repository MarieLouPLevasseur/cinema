
{% extends 'base.html.twig' %}

{% block body %}
    <!-- main -->

    <div class="container-xl bg-light p-5 border-top">

        <div class="row mb-2">
{# on inclus les catérogies #}
            {% include('front/movie/_genres_list.html.twig') %}

            

            <div class="col-12 col-lg-9">

                <h2 class="text-muted fw-bold mb-4">Les films et séries/Résultats de recherche</h2>
{# boucle en appelant le template pour l'affichage des films#}

{# on peut faire un tri direct des données avec la commande: 

                {#% for current_show in show_list|sort((a, b) => a.title <=> b.title) % #}

                {% for current_show in show_list %}

                    {% include ('front/movie/_show_detail.html.twig') %}

            {% endfor %}

            </div>

            {# <nav class="col-lg-9 offset-lg-3">
                <ul class="pagination">
                    <li class="page-item disabled">
                        <a class="page-link">&laquo;</a>
                    </li>
                    <li class="page-item active"><a class="page-link" href="#">1</a></li>
                    <li class="page-item" aria-current="page">
                        <a class="page-link" href="#">2</a>
                    </li>
                    <li class="page-item"><a class="page-link" href="#">3</a></li>
                    <li class="page-item">
                        <a class="page-link" href="#">&raquo;</a>
                    </li>
                </ul>
            </nav> #}

        </div>

    </div>

  {% endblock %}