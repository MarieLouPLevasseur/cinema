{% extends 'base.html.twig' %}

{% block title %}Demo Form!{% endblock %}

{% block body %}
<h1>Un exemple de formulaire</h1>
<div class="container">
    {{ form_start(form) }}
        {{ form_row(form.firstname) }}
        {{ form_row(form.lastname) }}
        <button class="btn btn-primary">Soumettre</button>
    {{ form_end(form) }}
</div>
{% endblock %}
