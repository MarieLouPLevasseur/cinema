{% extends 'base.html.twig' %}

{% block body %}

               {% include('back/_nav_backoffice.html.twig') %}
{# TODO Enregistre bien le genre en BDD mais ne l'associe pas au film #}

<h1> Liste des caractéristique du Genre : {{genre.id}} {{genre.name}}</h1>

<div class="container">
{# ! on force le no validate: on ne fait jamais confiance aux données utilisateur #}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {# {{ form_row(form.content) }} #}
        {{ form_rest(form) }}
        <button class="btn btn-primary">Soumettre</button>
    {{ form_end(form) }}
</div>
{% endblock %}